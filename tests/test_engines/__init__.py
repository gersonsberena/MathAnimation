# One test module per engine goes here as each is built (README Section
# 11.6, step 4) — param boundary tests at each declared param's min/max,
# per the Section 8 checklist. Not retrofitted for all 20 engines up front.
